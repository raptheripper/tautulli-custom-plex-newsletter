server {
    listen 80;

    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ =404;
    }

    location /newsletter/ {
        alias /newsletters/;
        try_files $uri =404;
    }
}
